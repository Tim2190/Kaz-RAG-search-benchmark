# --- ядро ---
numpy>=1.26

# --- сбор корпуса (Фаза 1) ---
requests>=2.31
beautifulsoup4>=4.12
trafilatura>=1.8          # извлечение текста статей с любых новостных сайтов
playwright>=1.40          # только для устаревшего gov_scraper (SPA-токены)

# --- лексический поиск (Фаза 3) ---
rank-bm25>=0.2.2

# --- dense-поиск: IBM Granite / Google LaBSE / multilingual-e5 (Фаза 3) ---
sentence-transformers>=2.7
torch>=2.1

# --- утилиты ---
scikit-learn>=1.3         # косинус/нормализация, опционально
tqdm>=4.66

# --- RAG: LLM generation (Colab/GPU recommended) ---
transformers>=4.40
accelerate>=0.29
bitsandbytes>=0.43          # 4-bit quantization for 7-9B models on T4

# --- query generation via Claude API ---
anthropic>=0.40

# --- HF Dataset export / upload ---
huggingface_hub>=0.23
datasets>=2.19              # optional: for programmatic HF upload
